package org.dreamcat.leetcode.daily.on2006;

import java.util.Collection;
import java.util.Collections;
import java.util.EnumMap;
import java.util.HashMap;
import java.util.PriorityQueue;
import java.util.concurrent.ConcurrentHashMap;
import java.util.concurrent.ConcurrentSkipListMap;
import java.util.concurrent.LinkedBlockingDeque;

/**
 * Create by tuke on 2020/6/28
 */
public class On28_sudoku {

    public static void main(String[] args) {
        PriorityQueue q;
        EnumMap e;
        ConcurrentHashMap c;
        HashMap h;
        Long l;
        Collections.synchronizedList(null);
        Collections.synchronizedMap(null);
        ConcurrentSkipListMap csm;
        LinkedBlockingDeque d;

        Collection col = null;
        col.stream();
        // 2147483649L
        for (long i = 1 << 31; ; ) {
            if (Long.hashCode(i++) < 0) {
                System.out.println(i);
                break;
            }
        }
    }

    // return
    private static int[][] getOrder(int[] seq) {

        for (int i = 0; i <= 8; i++) {

        }

        return null;

    }

    public void sukodu(int[][] m) {
        for (int i = 0; i <= 8; i++) {

            for (int j = 0; j <= 8; j++) {

            }
        }
    }

}
